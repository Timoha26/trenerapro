import {ConfModel} from "../models/conf.model";

export const conf: ConfModel = {
  //trainerProUrl: "http://trainerpro.local",
  trainerProUrl: "http://localhost:8000",
  filesUrl: "http://minio-api.local"
}
